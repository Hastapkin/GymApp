<Application x:Class="GymApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:memberViews="clr-namespace:GymApp.Views.Members"
             xmlns:membershipViews="clr-namespace:GymApp.Views.MembershipCards"
             xmlns:packageViews="clr-namespace:GymApp.Views.Packages"
             xmlns:staffViews="clr-namespace:GymApp.Views.Staffs"
             xmlns:memberVm="clr-namespace:GymApp.ViewModels.Member"
             xmlns:membershipVm="clr-namespace:GymApp.ViewModels.Membership"
             xmlns:packageVm="clr-namespace:GymApp.ViewModels.Packages"
             xmlns:staffVm="clr-namespace:GymApp.ViewModels.Staff"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- DataTemplates for ViewModels theo đúng README structure -->

        <!-- Member ViewModels -->
        <DataTemplate DataType="{x:Type memberVm:MemberListViewModel}">
            <memberViews:MemberListView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type memberVm:MemberCreateViewModel}">
            <memberViews:MemberCreateView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type memberVm:MemberEditViewModel}">
            <memberViews:MemberEditView/>
        </DataTemplate>

        <!-- Packages ViewModels -->
        <DataTemplate DataType="{x:Type packageVm:PackagesListViewModel}">
            <packageViews:PackagesListView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type packageVm:PackagesCreateViewModel}">
            <packageViews:PackagesCreateView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type packageVm:PackagesEditViewModel}">
            <packageViews:PackagesEditView/>
        </DataTemplate>

        <!-- MembershipCards ViewModels -->
        <DataTemplate DataType="{x:Type membershipVm:MembershipCardsListViewModel}">
            <membershipViews:MembershipCardsListView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type membershipVm:MembershipCardsCreateViewModel}">
            <membershipViews:MembershipCardsCreateView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type membershipVm:MembershipCardsEditViewModel}">
            <membershipViews:MembershipCardsEditView/>
        </DataTemplate>

        <!-- Staff ViewModels -->
        <DataTemplate DataType="{x:Type staffVm:StaffListViewModel}">
            <staffViews:StaffListView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type staffVm:StaffCreateViewModel}">
            <staffViews:StaffCreateView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type staffVm:StaffEditViewModel}">
            <staffViews:StaffEditView/>
        </DataTemplate>

        <!-- Global Styles -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="BorderBrush" Value="#2196F3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#BBDEFB"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#90CAF9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
        </Style>

        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A049"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
    </Application.Resources>
</Application>
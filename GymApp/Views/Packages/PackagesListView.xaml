<UserControl x:Class="GymApp.Views.Packages.PackagesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:GymApp.Helpers">
    <Grid>
        <ContentControl Content="{Binding CurrentView}"
                        Visibility="{Binding CurrentView, Converter={x:Static helpers:NullToVisibilityConverter.Instance}}"/>

        <Grid Visibility="{Binding CurrentView, Converter={x:Static helpers:NullToVisibilityConverter.Inverse}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Package Management" Style="{StaticResource HeaderTextStyle}"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
                <Button Content="Add Package" Command="{Binding CreatePackageCommand}"/>
                <Button Content="Edit Package" Command="{Binding EditPackageCommand}"/>
                <Button Content="Delete Package" Command="{Binding DeletePackageCommand}"/>
            </StackPanel>

            <DataGrid Grid.Row="2" ItemsSource="{Binding Packages}" SelectedItem="{Binding SelectedPackage}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Package Name" Binding="{Binding PackageName}" Width="200"/>
                    <DataGridTextColumn Header="Duration (Days)" Binding="{Binding DurationDays}" Width="120"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="120"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
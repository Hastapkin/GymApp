<UserControl x:Class="GymApp.Views.Staffs.StaffListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:GymApp.Helpers">
    <Grid>
        <ContentControl Content="{Binding CurrentView}"
                        Visibility="{Binding CurrentView, Converter={x:Static helpers:NullToVisibilityConverter.Instance}}"/>

        <Grid Visibility="{Binding CurrentView, Converter={x:Static helpers:NullToVisibilityConverter.Inverse}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Staff Management" Style="{StaticResource HeaderTextStyle}"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
                <Button Content="Add Staff" Command="{Binding CreateStaffCommand}"/>
                <Button Content="Edit Staff" Command="{Binding EditStaffCommand}"/>
                <Button Content="Delete Staff" Command="{Binding DeleteStaffCommand}"/>
            </StackPanel>

            <DataGrid Grid.Row="2" ItemsSource="{Binding Staff}" SelectedItem="{Binding SelectedStaff}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="200"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="120"/>
                    <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Salary" Binding="{Binding Salary, StringFormat=C}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>